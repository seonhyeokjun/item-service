spring:
  messages:
    basename: messages, errors # messages가 기본설정임

server:
  servlet:
    session:
      tracking-modes: cookie # cookie, url, ssl
      timeout: 30m # 30분

logging:
  level:
    org:
      apache:
        coyote:
#          http11: DEBUG
